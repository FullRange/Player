<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragment_list">

    <fragment
        android:id="@+id/fragment_list"
        android:name="com.oke.player.ui.main.list.ListFragment"
        tools:layout="@layout/fragment_list">

        <action
            android:id="@+id/navigate_to_player_view"
            app:destination="@+id/fragment_player" />

    </fragment>

    <fragment
        android:id="@+id/fragment_player"
        android:name="com.oke.player.ui.main.player.PlayerFragment"
        tools:layout="@layout/fragment_player">

    </fragment>

</navigation>
